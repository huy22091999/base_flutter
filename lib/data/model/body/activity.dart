import 'package:flutter/material.dart';

class Activity{
  DateTime dateTime;
  String title;
  String content;
  Color colorParent;
  Color colorChild;

  Activity(this.dateTime, this.title, this.content,this.colorChild,this.colorParent);
}